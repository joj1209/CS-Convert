# 회사 환경 디버깅 가이드

이 문서는 회사 환경(오프라인/폐쇄망)에서 변수 치환이 제대로 동작하지 않을 때 문제를 진단하기 위한 가이드입니다.

## 🔍 디버깅 단계

### 1️⃣ 브라우저 개발자 도구 활용

**단축키:**
- Chrome/Edge: `F12` 또는 `Ctrl + Shift + I`
- Firefox: `F12` 또는 `Ctrl + Shift + K`

### 2️⃣ Console 탭에서 실시간 확인

`index.html`의 `replaceOutsideStrings` 함수 내부에 다음 로그를 추가하여 실행 흐름을 추적합니다:

```javascript
function replaceOutsideStrings(sql, pairs, useWordBoundary) {
    console.log('=== 디버깅 시작 ===');
    console.log('입력 SQL:', sql);
    console.log('매핑 pairs:', pairs);
    console.log('skipStrings 옵션:', useWordBoundary);
    
    // 정규식 분리 결과 확인
    const segments = sql.split(/('(?:''|[^'])*')/g);
    console.log('분리된 세그먼트:', segments);
    
    for (let i = 0; i < segments.length; i++) {
      const type = i % 2 === 1 ? '(문자열-건너뜀)' : '(코드-치환대상)';
      console.log(`세그먼트[${i}]:`, segments[i], type);
      
      if (i % 2 === 1) continue;
      
      // ... (기존 로직) ...
    }
    // ...
}
```

### 3️⃣ 디버거 중단점(Breakpoint) 설정

1. 개발자 도구의 **Sources** 탭 클릭
2. 왼쪽 파일 탐색기에서 `index.html` 선택
3. `replaceOutsideStrings` 함수의 첫 줄 번호 클릭 (빨간 점 표시)
4. **변경버튼** 클릭 시 해당 위치에서 실행 멈춤
5. 단계별 실행:
   - `F10`: 다음 줄로 이동 (Step Over)
   - `F11`: 함수 내부로 진입 (Step Into)
   - `F8`: 계속 실행 (Resume)

### 4️⃣ 빠른 테스트 코드 (Console 입력)

개발자 도구 Console 탭에서 다음 코드를 직접 입력하여 정규식 동작을 확인합니다:

```javascript
// 테스트 1: 싱글쿼테이션 분리 확인
const testSQL = "SELECT @OLD_VAR1 FROM table WHERE name = '@OLD_VAR1'";
const segments = testSQL.split(/('(?:''|[^'])*')/g);
console.log('분리 결과:', segments);
// 예상 결과:
// [0]: "SELECT @OLD_VAR1 FROM table WHERE name = "
// [1]: "'@OLD_VAR1'"
// [2]: ""

// 테스트 2: 정규식 매칭 확인
const pattern = /('(?:''|[^'])*')/g;
console.log('매칭 결과:', testSQL.match(pattern));
// 예상 결과: ["'@OLD_VAR1'"]
```

### 5️⃣ 문제 원인 체크리스트

회사 환경에서 확인해야 할 사항들:

- [ ] **브라우저 버전**: Chrome 62+ 이상인지 확인 (Lookbehind 정규식 지원 필요)
- [ ] **파일 인코딩**: 파일이 UTF-8로 저장되었는지 확인
- [ ] **특수문자**: 복사/붙여넣기 시 따옴표가 스마트 따옴표(`’`)로 변환되지 않았는지 확인
- [ ] **옵션 상태**: `skipStrings` 체크박스가 의도대로 설정되었는지 확인

### 6️⃣ 별도 디버깅 파일 사용

`debug-version.html` 파일을 사용하여 최소한의 환경에서 테스트를 수행합니다. 이 파일은 복잡한 로직을 제거하고 핵심 정규식 동작만 테스트합니다.

---
**작성일**: 2025-12-14
